# Project memory

This folder is Chiron's canonical project memory — the decisions, architecture and conventions distilled from past sessions, written as readable, git-versioned markdown. It is the auditable half of the hybrid memory; the long tail lives in RAG (the `memory_search` tool).

It grows automatically as tasks are delivered. Read this map first, then open the relevant concept file.

## Sections

_(empty — grows as Chiron distills memory)_

