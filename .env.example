NEXT_PUBLIC_CATALOG_API_URL=http://localhost:8001
NEXT_PUBLIC_CHECKOUT_API_URL=http://localhost:8002

# OPENAI_API_KEY is optional and does NOT need to be set here.
# The AI chat assistant reads the OpenAI key per-user, configured via Settings.
